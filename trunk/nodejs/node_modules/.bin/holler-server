#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../holler/bin/holler-server" "$@"
  ret=$?
else 
  node  "$basedir/../holler/bin/holler-server" "$@"
  ret=$?
fi
exit $ret
